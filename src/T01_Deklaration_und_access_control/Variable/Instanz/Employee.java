package T01_Deklaration_und_access_control.Variable.Instanz;
// Instanz variable wird drin die Klasse deklariert aber außerhalb die Methode
public class Employee {
    // instanz variable (name, titel, position, manager) kann alles mit die "access-modifier" markieren
    public      String name;
    private     String titel;
    /*default*/ String position;
    protected   String manager;
}
